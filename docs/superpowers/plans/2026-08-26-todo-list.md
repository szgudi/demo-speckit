# Todo List Implementation Plan

> **For agentic workers:** Implement task-by-task from `specs/001-todo-list/tasks.md`, preserving its dependency order and verification gates.

**Goal:** Build the specified Chinese React Todo List backed by a Node.js API and MySQL.

**Architecture:** pnpm workspace with shared Zod contracts, Express route/service/repository layers, and a React client using HTTP only. API and E2E tests use a dedicated `_test` MySQL database.

**Tech Stack:** Node.js 20, TypeScript strict mode, React 18, Vite, Express, mysql2, Zod, Vitest, Testing Library, Supertest, MSW, Playwright, MySQL 8.

## Execution

Implement T001–T046 in `specs/001-todo-list/tasks.md`. Each phase must pass its listed command before the next phase; write tests before production behavior, keep all external inputs as `unknown` until schema validation, and update the task checkboxes only after fresh verification.
